#pragma once

class Node;

////////////////////////////////////////////////////////////////////////
///@brief 边类，含两个节点的引用
////////////////////////////////////////////////////////////////////////

struct Side
{
	Side(const Node& n1, const Node& n2);

	//@brief 判断是否割边
	bool is_cut();

	const Node& node1;
	const Node& node2;
};

